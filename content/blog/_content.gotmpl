{{/* Blog content adapter. One file per language (translation by filename, same
     rule as content); the shared logic lives in the partial so the three stay
     in sync. See layouts/partials/auto-untranslated-pages.html. */}}
{{ partial "auto-untranslated-pages.html" . }}
